# DEPLOYMENT_STRATEGY — Green Vita AI Plant Clinic

> این مستند فقط تحلیل و توصیه است. **هیچ کدی تغییر نکرده و هیچ commit ای انجام نشده.**
> هدف: انتخاب استراتژی دیپلوی برای نسخه‌ی پروداکشن‌گرِید که قرار است به بیش از ۱۰۰,۰۰۰
> کاربر برسد.

## پیش‌فرض فنی کلیدی که باید قبل از هر مقایسه‌ای روشن باشد

Telegram Bot API فقط اجازه می‌دهد **یک consumer** روی `getUpdates` (یعنی حالت polling) برای
هر توکن بات فعال باشد؛ اگر دو پروسه هم‌زمان با همان توکن polling کنند، تلگرام برای یکی از
آن‌ها خطای `409 Conflict` برمی‌گرداند. این یعنی **در حالت polling، معماری بات ذاتاً روی یک
نمونه (single instance) قفل است** — هرچقدر هم زیرساخت زیرش قوی باشد. این محدودیت روی هر سه
گزینه‌ی پایین که «Polling» دارند اثر می‌گذارد و مهم‌ترین فاکتور تصمیم‌گیری برای مقیاس
۱۰۰,۰۰۰+ کاربر است.

در مقابل، **Webhook** یعنی تلگرام خودش هر Update را به‌صورت یک HTTP POST مستقل به سرور شما
می‌فرستد — یعنی هیچ محدودیت single-consumer ای وجود ندارد و می‌توان همزمان چند نمونه از
سرویس را برای پردازش Updateهای مختلف بالا نگه داشت.

---

## گزینه ۱: Telegram Polling روی VPS

| معیار | ارزیابی |
|---|---|
| **مزایا** | کنترل کامل روی سرور؛ بدون cold start؛ کد فعلی پروژه بدون هیچ تغییری کار می‌کند؛ هزینه‌ی قابل‌پیش‌بینی و ثابت؛ نیازی به دامنه/HTTPS/webhook secret نیست؛ دیباگ و SSH مستقیم ساده است |
| **معایب** | مقیاس‌پذیری بات به یک نمونه محدود است (محدودیت بالا)؛ patch امنیتی OS، مانیتورینگ، HA، zero-downtime deploy همه دستی‌اند؛ Single Point of Failure مگر خودت HA بسازی (که پیچیدگی اضافه می‌کند) |
| **هزینه** | قابل‌پیش‌بینی، پله‌ای (مثلاً یک VPS با ۲-۴ گیگ رم کافی است)، اما برای مقیاس بزرگ باید دستی سایز سرور را بالا ببری یا معماری را جدا کنی |
| **مقیاس‌پذیری** | پردازش عکس→AI را می‌توان با صف (Redis Queue/Celery) از پروسه‌ی polling جدا کرد (نیاز به تغییر کد)، ولی خودِ polling همیشه تک‌نمونه باقی می‌ماند |
| **پایداری/Reliability** | به انضباط عملیاتی خودت وابسته است؛ بدون SLA؛ ریستارت خودکار روی کرش نیاز به تنظیم دستی (systemd/Docker restart policy) دارد |
| **پیچیدگی** | شروع ساده، ولی با رشد پروژه (HA، امنیت، مانیتورینگ، deploy بدون downtime) پیچیدگی عملیاتی به‌صورت دستی روی دوش تیم می‌افتد |
| **تغییر کد لازم** | **هیچ** — معماری فعلی دقیقاً همین است |

## گزینه ۲: Telegram Polling روی Railway

| معیار | ارزیابی |
|---|---|
| **مزایا** | Deploy با `git push`؛ Postgres/Redis به‌صورت add-on مدیریت‌شده؛ داشبورد لاگ/متریک آماده؛ overhead عملیاتی کمتر از VPS خام؛ برای MVP سریع مناسب است |
| **معایب** | همان محدودیت single-consumer polling را دارد — Railway این محدودیت ذاتی تلگرام را حل نمی‌کند؛ کنترل کمتر روی زیرساخت (ریجن، شبکه) نسبت به VPS/Cloud Run؛ وابستگی به یک پلتفرم PaaS با بلوغ/SLA کمتر از یک Cloud Provider بزرگ |
| **هزینه** | مبتنی بر مصرف (CPU/RAM/شبکه)، ولی چون بات باید ۲۴/۷ روشن بماند (polling)، عملاً شبیه هزینه‌ی یک VPS کوچک است + پرمیوم مدیریت‌شدگی؛ در مقیاس بزرگ می‌تواند از VPS خام گران‌تر شود |
| **مقیاس‌پذیری** | دقیقاً همان سقف polling؛ اتوسکیل Railway هم کمکی به این محدودیت نمی‌کند چون چند نمونه هم‌زمان با یک توکن ممکن نیست |
| **پایداری/Reliability** | برای پروژه‌های کوچک-متوسط قابل‌قبول؛ فاقد بلوغ/SLA زیرساخت‌های بزرگ مثل GCP/AWS؛ کنترل کمتر در زمان outage پلتفرم |
| **پیچیدگی** | کمترین پیچیدگی برای شروع، ولی در outage پلتفرم اهرم کنترلی کمی داری |
| **تغییر کد لازم** | **هیچ** (شبیه VPS) |

## گزینه ۳: Telegram Webhook روی Cloud Run

| معیار | ارزیابی |
|---|---|
| **مزایا** | مقیاس‌پذیری افقی واقعی (بدون محدودیت single-consumer)؛ scale-to-zero وقتی ترافیک نیست؛ HTTPS/TLS مدیریت‌شده؛ زیرساخت با SLA گوگل؛ دقیقاً همان مدل عملیاتی‌ای که پنل مدیریت پروژه (`admin-service.yaml`) از قبل استفاده می‌کند — یکپارچگی عملیاتی؛ CI/CD این پروژه از قبل برای Cloud Run آماده است؛ blue/green ساده با traffic splitting بین revisionها |
| **معایب** | نیاز به تغییر کد (پایین توضیح داده شده)؛ نیاز به دامنه/HTTPS عمومی (Cloud Run خودش می‌دهد)؛ نیاز به `secret_token` برای امنیت webhook؛ باید connection pool دیتابیس برای چند نمونه‌ی هم‌زمان تنظیم شود؛ cold start ممکن است (با `minScale=1` قابل رفع با هزینه‌ی کم) |
| **هزینه** | Pay-per-use — در ترافیک کم تقریباً رایگان (scale-to-zero)، در ترافیک بالا هزینه با مصرف واقعی رشد می‌کند؛ بهترین نسبت هزینه-به-قابلیت‌اطمینان برای ترافیکی که با رشد کاربر نوسان دارد (که دقیقاً وضعیت یک بات در حال رشد تا ۱۰۰هزار کاربر است) |
| **مقیاس‌پذیری** | **بهترین گزینه** — اتوسکیل افقی مدیریت‌شده توسط گوگل تا هر تعداد نمونه که تنظیم کنی، بدون محدودیت معماری polling |
| **پایداری/Reliability** | **بالاترین** — SLA گوگل، health-check و جایگزینی خودکار نمونه‌های ناسالم، ادغام بومی با Cloud Monitoring/Logging/Alerting |
| **پیچیدگی** | راه‌اندازی اولیه پیچیده‌تر (migration زیر را ببین)، ولی پیچیدگی عملیاتی مداوم (patch سرور، HA دستی) کاملاً حذف می‌شود |
| **تغییر کد لازم** | **بله — قابل‌توجه ولی محدود و مشخص** (بخش migration plan را ببین) |

---

## جدول مقایسه‌ی خلاصه

| معیار | VPS Polling | Railway Polling | Cloud Run Webhook |
|---|---|---|---|
| مقیاس‌پذیری افقی بات | ❌ (سقف: ۱ نمونه) | ❌ (سقف: ۱ نمونه) | ✅ (نامحدود، اتوسکیل) |
| Reliability/SLA | خودت مسئولی | متوسط | بالا (SLA گوگل) |
| هزینه در ترافیک کم | ثابت (حتی وقتی بیکار) | ثابت (حتی وقتی بیکار) | تقریباً صفر (scale-to-zero) |
| هزینه در ترافیک بسیار بالا (۱۰۰هزار+ کاربر) | قابل‌پیش‌بینی ولی نیازمند بازطراحی معماری | معمولاً گران‌تر از VPS | مقیاس‌پذیر با مصرف واقعی |
| overhead عملیاتی مداوم | بالا (دستی) | کم-متوسط | خیلی کم |
| تغییر کد لازم | هیچ | هیچ | بله (مشخص و محدود) |
| هم‌راستا با زیرساخت فعلی پروژه | خیر (Docker/Compose موجود است ولی جدا از Cloud Run) | خیر | **بله** (CI/CD و admin-service از قبل روی Cloud Run هستند) |

---

## توصیه‌ی نهایی

**Telegram Webhook روی Cloud Run** — تنها گزینه‌ای که محدودیت ذاتی معماری (سقف تک‌نمونه‌ای
polling) را از بین می‌برد و واقعاً برای رشد به بیش از ۱۰۰,۰۰۰ کاربر طراحی شده است.

**چرا نه VPS و نه Railway:** هر دو، صرف‌نظر از قدرت زیرساخت، بات را روی **یک نمونه‌ی واحد**
از polling قفل نگه می‌دارند. در مقیاس ۱۰۰هزار+ کاربر، وقتی صدها/هزاران Update (پیام، عکس)
هم‌زمان می‌رسند، این تک‌نمونه به‌مرور به گلوگاه پردازشی (bottleneck) تبدیل می‌شود — چه روی
VPS باشد چه روی Railway، تفاوتی نمی‌کند، چون محدودیت از تلگرام می‌آید نه از زیرساخت.
افزودن HA/opsهای لازم برای جبران این محدودیت (صف پردازش، چند worker پشت یک polling واحد)
عملاً همان میزان تغییر کد را نیاز دارد که مهاجرت به webhook نیاز دارد — ولی بدون رسیدن به
مقیاس‌پذیری واقعی webhook.

**چرا Cloud Run و نه یک ارائه‌دهنده‌ی دیگر برای webhook:** پروژه از قبل CI/CD و مانیفست
Cloud Run برای پنل مدیریت را دارد؛ مهاجرت بات به همین مدل، هزینه‌ی یادگیری/ابزار جدید صفر
است و یک مدل عملیاتی واحد برای هر دو سرویس ایجاد می‌کند.

---

## نقشه‌ی راه مهاجرت از Polling به Webhook

طبق قانون «فیچرهای بزرگ را فاز به فاز پیاده کن و قبل از ادامه تأیید بگیر»، این مهاجرت به
۴ فاز مستقل تقسیم شده. **هیچ‌کدام از این فازها هنوز اجرا نشده‌اند — این فقط نقشه‌ی راه است.**

### فاز A — افزودن مسیر Webhook در کنار Polling موجود (بدون حذف چیزی)

هدف: کد فعلی دست‌نخورده بماند، فقط یک مسیر جایگزین اضافه شود که با یک متغیر محیطی
(مثلاً `BOT_RUN_MODE=polling|webhook`) قابل انتخاب باشد — یعنی رول‌بک همیشه فقط یک تغییر env
است، نه revert کد.

تغییرات مشخص (فقط برای برنامه‌ریزی، هنوز پیاده نشده):
- افزودن یک HTTP endpoint (با `aiohttp` که خودِ aiogram برایش `SimpleRequestHandler` آماده
  دارد، یا یک route داخل همان الگوی FastAPI که پنل ادمین استفاده می‌کند) که Updateهای
  دریافتی از تلگرام را می‌گیرد و به همان `Dispatcher` فعلی پاس می‌دهد — یعنی همه‌ی
  هندلرها/middlewareهای موجود (`LoggingMiddleware`, `DBSessionMiddleware`, همه‌ی روترها)
  بدون هیچ تغییری دوباره استفاده می‌شوند.
- افزودن `WEBHOOK_SECRET_TOKEN` (تولید تصادفی) برای اعتبارسنجی اینکه درخواست واقعاً از
  تلگرام می‌آید (پارامتر استاندارد `secret_token` در `setWebhook` و بررسی هدر
  `X-Telegram-Bot-Api-Secret-Token`).
- افزودن یک `main_webhook.py` یا شاخه‌ی شرطی در همان `src/bot/main.py` که بر اساس
  `BOT_RUN_MODE`، یا `dp.start_polling(bot)` را صدا می‌زند (رفتار فعلی) یا سرور HTTP webhook
  را بالا می‌آورد.

خروجی این فاز: در محیط dev/staging با `BOT_RUN_MODE=webhook` قابل تست است، در پروداکشن فعلی
هیچ تغییری حس نمی‌شود چون پیش‌فرض همچنان `polling` می‌ماند.

**⛔️ توقف و تأیید لازم قبل از فاز B.**

### فاز B — آماده‌سازی زیرساخت Cloud Run برای Webhook

- به‌روزرسانی `cloudrun/bot-service.yaml`: حذف محدودیت `minScale=1`/`maxScale=1` (که فقط
  برای polling لازم بود)، افزودن `containerPort` متناظر با پورت HTTP جدید، تنظیم
  `minScale` روی ۰ یا ۱ بسته به تصمیم درباره‌ی cold start، و `maxScale` متناسب با تخمین بار.
- ذخیره‌ی `WEBHOOK_SECRET_TOKEN` در Secret Manager.
- بررسی و تنظیم connection pool دیتابیس (`src/db/session.py` — مقادیر `pool_size`/
  `max_overflow` فعلی برای یک پروسه‌ی تک‌نمونه تنظیم شده‌اند؛ برای چند نمونه‌ی هم‌زمان Cloud
  Run باید متناسب با تعداد نمونه‌ها و سقف کانکشن Postgres بازبینی شوند — یا از یک connection
  pooler مثل PgBouncer/Cloud SQL Proxy استفاده شود).
- تصمیم درباره‌ی دامنه: استفاده از دامنه‌ی پیش‌فرض `*.run.app` (ساده‌تر) یا دامنه‌ی
  اختصاصی (نیاز به mapping دستی).

**⛔️ توقف و تأیید لازم قبل از فاز C.**

### فاز C — Cutover کنترل‌شده

- Deploy نسخه‌ی جدید با `BOT_RUN_MODE=webhook` ولی **بدون** فراخوانی `setWebhook` هنوز —
  فقط برای اطمینان از بالا آمدن سالم سرویس (`/health` را چک کن).
- فراخوانی `bot.set_webhook(url=..., secret_token=..., drop_pending_updates=True)` یک‌بار
  (دستی یا با یک اسکریپت کوچک) تا تلگرام رسماً شروع به ارسال Update به URL جدید کند.
- مانیتورینگ فعال حین گذار: نرخ خطای Cloud Run، لاگ‌های `LoggingMiddleware`، تأخیر پاسخ‌دهی.
- **پلن Rollback آماده:** اگر مشکلی دیده شد، `bot.delete_webhook()` را صدا بزن و سرویس را
  با `BOT_RUN_MODE=polling` دوباره دیپلوی کن — چون کد polling دست‌نخورده باقی مانده (فاز A).

**⛔️ توقف و تأیید لازم قبل از فاز D.**

### فاز D — پاکسازی و سخت‌سازی نهایی

- بعد از دوره‌ی اطمینان پایدار (پیشنهاد: حداقل ۱-۲ هفته بدون incident)، مسیر polling را از
  کد حذف کن (یا فقط برای dev لوکال نگه دار) و `BOT_RUN_MODE` را دیگر لازم نداشته باش.
- Load test با ترافیک شبیه‌سازی‌شده در مقیاس هدف (۱۰۰هزار+ کاربر) برای تنظیم دقیق
  `maxScale`، `containerConcurrency`، و connection pool.
- افزودن Alerting (Cloud Monitoring) روی نرخ خطای webhook و تأخیر پاسخ.
- بازبینی `KNOWN_ISSUES.md` — موارد C2 (ناسازگاری بات با Cloud Run Service) و H6 (اتصال
  Redis از Cloud Run) بعد از این مهاجرت باید بسته شوند.

---

## جمع‌بندی یک‌خطی

برای پروژه‌ای که قرار است به ۱۰۰,۰۰۰+ کاربر برسد، **Polling** — چه روی VPS چه روی Railway —
یک سقف معماری غیرقابل‌دورزدن دارد؛ **Webhook روی Cloud Run** تنها گزینه‌ای است که هم با
زیرساخت فعلی پروژه هم‌راستاست و هم واقعاً بدون محدودیت افقی مقیاس می‌گیرد. نقشه‌ی راه بالا
این مهاجرت را به ۴ فاز کم‌ریسک و قابل‌رول‌بک تقسیم کرده — منتظر تأیید تو برای شروع فاز A هستم.
