# RELEASE_NOTES — v0.1-baseline

تاریخ: بر اساس چهارمین ممیزی/سخت‌سازی پروژه، قبل از هر مهاجرت معماری.
معماری: **Telegram Polling** (بدون تغییر نسبت به فازهای قبلی).

## فیچرهای پیاده‌سازی‌شده تا این نسخه

| فیچر | وضعیت |
|---|---|
| اسکلت پروژه (aiogram 3 + FastAPI + Repository Pattern + AI Provider abstraction) | ✅ |
| تشخیص بیماری گیاه از روی عکس (عکس + سؤال از کاربر → AI → نتیجه) | ✅ |
| زنجیره‌ی ساختاریافته‌ی «۱-تشخیص ← ۲-علت ← ۳-درمان» | ✅ |
| شناسایی گونه‌ی گیاه از روی عکس (نام فارسی/علمی + راهنمای کامل نگهداری) | ✅ |
| دکمه‌ی «درخواست ویزیت متخصص گرین‌ویتا» + اطلاع‌رسانی خودکار به ادمین‌ها | ✅ |
| پرش مستقیم از نتیجه‌ی شناسایی گیاه به فلوی تشخیص بیماری (بدون تکرار سؤال) | ✅ |
| پنل مدیریت پایه (داشبورد آماری + health check) | ✅ (بدون احراز هویت) |
| سوییچ پروایدر AI (Claude/Gemini/OpenAI) فقط با متغیر محیطی | ✅ |
| Docker + docker-compose (db, redis, migrate, bot, admin) | ✅ |
| ۵ مایگریشن Alembic (`0001` تا `0005`)، زنجیره‌ی خطی و سالم | ✅ |
| CI/CD پایه با GitHub Actions (lint, test, build, push, deploy) | ✅ |

## اصلاحات Startup در این نسخه

- **`src/bot/main.py`:** اعتبارسنجی پیکربندی AI Provider (`get_ai_provider()`) بلافاصله بعد
  از چک `BOT_TOKEN`، هنگام استارت بات اضافه شد. قبل از این اصلاح، اگر `AI_PROVIDER` یا کلید
  API متناظرش غلط/خالی بود، بات ظاهراً سالم بالا می‌آمد و مشکل فقط وقتی اولین کاربر عکس
  می‌فرستاد آشکار می‌شد. الان کانفیگ خراب = شکست فوری و واضح در لاگ استارت، نه شکست خاموش
  برای کاربر واقعی.
- هیچ تغییر رفتاری دیگری در این نسخه اعمال نشده — هیچ فیچر جدید، هیچ ریفکتور.

## آمادگی برای Deploy

- ✅ سینتکس کل پروژه (`py_compile`) تمیز
- ✅ زنجیره‌ی مایگریشن‌ها خطی و بدون شکاف (`0001→0005`)
- ✅ همه‌ی importهای شخص‌ثالث با `requirements.txt` مطابقت دارند
- ✅ همه‌ی فیلدهای `Settings` با `.env.example` یک‌به‌یک تطبیق دارند
- ✅ ساختار پکیج‌ها (`__init__.py`) در همه‌ی زیرپوشه‌های `src/` کامل است
- ✅ `docker-compose.yml` شامل هر ۵ سرویس لازم (`db`, `redis`, `migrate`, `bot`, `admin`) با
  ترتیب `depends_on`/healthcheck صحیح برای معماری polling فعلی
- 📄 مراحل کامل دیپلوی و لیست secretهای لازم: `DEPLOY_CHECKLIST.md`

⚠️ این آمادگی از طریق بررسی دقیق کد (سینتکس، importها، منطق، پیکربندی) تأیید شده، **نه با
اجرای واقعی در یک محیط زنده** (محیط تولید این مستندات به اینترنت/نصب پکیج دسترسی ندارد).
پیشنهاد قطعی: قبل از اعلام عمومی v0.1، یک بار «تست دود» کامل طبق بخش ۶ فایل
`DEPLOY_CHECKLIST.md` روی محیط واقعی انجام شود.

## محدودیت‌های شناخته‌شده (غیربلاک‌کننده برای MVP polling)

فهرست کامل و اولویت‌بندی‌شده در `docs/KNOWN_ISSUES.md`؛ مهم‌ترین‌ها برای این نسخه:

- 🔴 **پنل مدیریت هیچ احراز هویتی ندارد** — تا اضافه‌شدن لاگین، دسترسی شبکه‌ای پنل را با
  فایروال/VPN/IP allowlist محدود نگه‌دار؛ URL آن را عمومی تبلیغ نکن.
- 🟠 هیچ Rate Limiting روی درخواست‌های AI نیست — مصرف API پروایدر انتخابی را روزانه رصد کن.
- 🟠 fallback از Redis به MemoryStorage در استارت بات عملاً تست اتصال واقعی انجام نمی‌دهد؛
  قطعی Redis فقط وسط یک مکالمه‌ی کاربر خودش را نشان می‌دهد، نه در لاگ استارت.
- 🟡 مدل‌های `Plant`, `Conversation`, `Reminder` در دیتابیس تعریف شده‌اند ولی هیچ هندلری
  از آن‌ها استفاده نمی‌کند (عدد «تعداد گیاهان» در داشبورد همیشه ۰ است).
- 🟡 `Diagnosis.plant_id` همیشه `NULL` است (بدون اتصال رسمی به پرونده‌ی گیاه).

**این محدودیت‌ها مانع دیپلوی معماری polling فعلی نیستند** — همگی یا مربوط به فیچرهای هنوز
پیاده‌نشده‌اند یا نیازمند اقدام عملیاتی (نه کد) هستند.

## فیچرهای هنوز پیاده‌نشده

- گفتگوی آزاد/تخصصی گیاه‌پزشکی (مدل `Conversation` آماده، هندلر ندارد)
- پرونده‌ی رسمی گیاه — ثبت/مدیریت (مدل `Plant` آماده، هندلر ندارد)
- یادآوری آبیاری/کوددهی (مدل `Reminder` آماده، Scheduler ندارد)
- فروشگاه و پیشنهاد محصول
- احراز هویت پنل مدیریت
- مدیریت کامل کاربران/تشخیص‌ها/درخواست‌های ویزیت از داخل پنل

## پلتفرم دیپلوی توصیه‌شده برای این نسخه (v0.1، معماری Polling)

طبق تحلیل کامل در `DEPLOYMENT_STRATEGY.md`: **یک VPS ساده یا سرور با `docker-compose`**
(گزینه‌ی A). دلیل: معماری polling فعلی روی یک نمونه قفل است (محدودیت خودِ Telegram Bot
API، نه زیرساخت) — بنابراین صرف هزینه‌ی بیشتر برای زیرساخت مدیریت‌شده در این مرحله ارزش
افزوده‌ی مقیاس‌پذیری نمی‌دهد. Cloud Run فقط برای پنل مدیریت (که HTTP معمولی است) مناسب است،
نه برای خودِ بات در حالت فعلی‌اش.

> برای رشد به بیش از ۱۰۰,۰۰۰ کاربر، مسیر **Webhook روی Cloud Run** طبق نقشه‌ی راه ۴فازی
> `DEPLOYMENT_STRATEGY.md` توصیه شده — ولی این مهاجرت هنوز شروع نشده و بخشی از این نسخه‌ی
> baseline نیست.
